# Copy this file to .env.local for local builds if you want to preserve
# a stable bundle identifier and signing identity across rebuilds.
#
# Do not commit .env.local.

export FACEUNLOCK_BUNDLE_ID="com.example.FaceUnlock"
export FACEUNLOCK_SIGNING_IDENTITY="Apple Development: Your Name (TEAMID)"

